import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: '/Tanho-konstovar/',   // 👈 GitHub repo nomini shu yerga yozasiz!
})
